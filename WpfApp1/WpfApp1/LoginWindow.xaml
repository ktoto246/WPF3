<Window x:Class="WpfApp1.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp1"
        mc:Ignorable="d"
        Title="Авторизация — Отделение заготовки крови №1" 
        Height="450" Width="400"
        WindowStyle="None" 
        AllowsTransparency="True" 
        Background="Transparent"
        WindowStartupLocation="CenterScreen"
        MouseDown="Window_MouseDown">

    <Window.Resources>
        <Style x:Key="ModernField" TargetType="Control">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="Margin" Value="0,0,0,15"/>
            <Setter Property="Background" Value="#F8F9FA"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>
    </Window.Resources>

    <Border Background="White" CornerRadius="12" BorderThickness="1" BorderBrush="#D1D5DB">
        <Border.Effect>
            <DropShadowEffect BlurRadius="15" Direction="270" ShadowDepth="3" Opacity="0.15"/>
        </Border.Effect>
        
        <Grid Margin="30">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Button Content="✕" HorizontalAlignment="Right" VerticalAlignment="Top" 
                    Background="Transparent" BorderBrush="Transparent" Foreground="#9CA3AF"
                    FontSize="16" Cursor="Hand" Click="CloseButton_Click" Focusable="False"/>

            <StackPanel Grid.Row="1" Margin="0,10,0,30" HorizontalAlignment="Center">
                <TextBlock Text="СЛУЖБА КРОВИ" FontSize="24" FontWeight="Bold" Foreground="#DC2626" HorizontalAlignment="Center"/>
                <TextBlock Text="Система учета доноров и запасов крови" FontSize="12" Foreground="#6B7280" Margin="0,5,0,0" HorizontalAlignment="Center"/>
            </StackPanel>

            <StackPanel Grid.Row="2" VerticalAlignment="Center">
                <TextBlock Text="Логин (Email / Телефон)" FontSize="12" FontWeight="SemiBold" Foreground="#374151" Margin="0,0,0,5"/>
                <TextBox x:Name="UsernameBox" Style="{StaticResource ModernField}"/>

                <TextBlock Text="Пароль" FontSize="12" FontWeight="SemiBold" Foreground="#374151" Margin="0,0,0,5"/>
                <PasswordBox x:Name="PasswordBox" Style="{StaticResource ModernField}"/>

                <TextBlock x:Name="ErrorText" Foreground="#DC2626" FontSize="12" TextWrapping="Wrap" 
                           HorizontalAlignment="Center" TextAlignment="Center" Margin="0,5,0,0" MinHeight="18"/>
            </StackPanel>

            <Button Grid.Row="3" x:Name="LoginButton" Content="Войти в систему" 
                    Height="45" Background="#DC2626" Foreground="White" FontSize="14" FontWeight="Bold"
                    BorderBrush="Transparent" Cursor="Hand" Click="LoginButton_Click">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="8"/>
                    </Style>
                </Button.Resources>
            </Button>
        </Grid>
    </Border>
</Window>