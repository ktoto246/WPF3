<Page x:Class="WpfApp1.Pages.LaboratoryTestsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp1.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
     Title="Лабораторные исследования" Loaded="Page_Loaded" Background="#F3F4F6">

    <Page.Resources>
        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="Margin" Value="0,0,0,12"/>
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="BorderBrush" Value="#9CA3AF"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="6">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModernComboBox" TargetType="ComboBox">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="Margin" Value="0,0,0,12"/>
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="BorderBrush" Value="#9CA3AF"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <Style x:Key="ModernDatePicker" TargetType="DatePicker">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Margin" Value="0,0,0,12"/>
            <Setter Property="BorderBrush" Value="#9CA3AF"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <Style TargetType="TextBlock" x:Key="FormLabel">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#4B5563"/>
            <Setter Property="Margin" Value="0,0,0,5"/>
        </Style>

        <Style TargetType="Button" x:Key="ActionBtn">
            <Setter Property="Background" Value="#E5E7EB"/>
            <Setter Property="Foreground" Value="#374151"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#D1D5DB"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Button" x:Key="PrimaryBtn" BasedOn="{StaticResource ActionBtn}">
            <Setter Property="Background" Value="#DC2626"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#B91C1C"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="6.5*"/>
            <ColumnDefinition Width="3.5*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="White" CornerRadius="10" Margin="0,0,10,0">
            <Border.Effect>
                <DropShadowEffect BlurRadius="10" Direction="270" ShadowDepth="2" Opacity="0.05"/>
            </Border.Effect>

            <Grid Margin="15">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Text="Журнал лабораторных исследований" FontSize="18" FontWeight="Bold" Foreground="#1F2937" Margin="0,0,0,15"/>

                <DataGrid x:Name="TestsGrid" Grid.Row="1" AutoGenerateColumns="False" 
                          IsReadOnly="True" HeadersVisibility="Column" GridLinesVisibility="Horizontal" 
                          HorizontalGridLinesBrush="#E5E7EB" BorderThickness="0"
                          RowBackground="White" AlternatingRowBackground="#F9FAFB" 
                          RowHeight="40" Background="Transparent" Foreground="#374151"
                          ScrollViewer.HorizontalScrollBarVisibility="Auto" ScrollViewer.VerticalScrollBarVisibility="Auto">
                    <DataGrid.Resources>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="#F3F4F6"/>
                            <Setter Property="FontWeight" Value="SemiBold"/>
                            <Setter Property="Foreground" Value="#4B5563"/>
                            <Setter Property="Padding" Value="10,8"/>
                        </Style>
                    </DataGrid.Resources>
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Дата тестирования *" Binding="{Binding TestDate, StringFormat=\{0:dd.MM.yyyy\}}" MinWidth="130"/>
                        <DataGridTextColumn Header="Донация *" Binding="{Binding Donation.DonationNumber}" MinWidth="130"/>
                        <DataGridTextColumn Header="Лаборант" Binding="{Binding Employee.FullName}" MinWidth="150"/>

                        <DataGridTextColumn Header="ИФА ВИЧ *" Binding="{Binding HIV_Result}" MinWidth="110"/>
                        <DataGridTextColumn Header="ИФА HBsAg *" Binding="{Binding HBsAg_Result}" MinWidth="110"/>
                        <DataGridTextColumn Header="ИФА HCV *" Binding="{Binding HCV_Result}" MinWidth="110"/>
                        <DataGridTextColumn Header="Сифилис *" Binding="{Binding Syphilis_Result}" MinWidth="110"/>

                        <DataGridTextColumn Header="НАТ ВИЧ" Binding="{Binding NAT_HIV}" MinWidth="110"/>
                        <DataGridTextColumn Header="НАТ HBV" Binding="{Binding NAT_HBV}" MinWidth="110"/>
                        <DataGridTextColumn Header="НАТ HCV" Binding="{Binding NAT_HCV}" MinWidth="110"/>

                        <DataGridTextColumn Header="АЛТ (Ед/л)" Binding="{Binding AltUL, StringFormat=\{0:F1\}}" MinWidth="90"/>
                        <DataGridTextColumn Header="Итоговый результат *" Binding="{Binding OverallResult}" MinWidth="160"/>
                        <DataGridTextColumn Header="Примечание" Binding="{Binding Notes}" Width="*" MinWidth="150"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>

        <Border Grid.Column="1" Background="White" CornerRadius="10" Margin="10,0,0,0">
            <Border.Effect>
                <DropShadowEffect BlurRadius="10" Direction="270" ShadowDepth="2" Opacity="0.05"/>
            </Border.Effect>

            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel Margin="20">
                    <TextBlock Text="Внесение результатов" FontSize="18" FontWeight="Bold" Foreground="#1F2937" Margin="0,0,0,15"/>

                    <TextBlock Text="Донация *" Style="{StaticResource FormLabel}"/>
                    <ComboBox x:Name="CmbDonation" Style="{StaticResource ModernComboBox}" DisplayMemberPath="DonationNumber" SelectedValuePath="DonationId"/>

                    <TextBlock Text="Дата тестирования *" Style="{StaticResource FormLabel}"/>
                    <DatePicker x:Name="DpTestDate" Style="{StaticResource ModernDatePicker}"/>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="10"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0">
                            <TextBlock Text="ИФА ВИЧ *" Style="{StaticResource FormLabel}"/>
                            <ComboBox x:Name="CmbHIV" Style="{StaticResource ModernComboBox}">
                                <ComboBoxItem Content="Отрицательный"/>
                                <ComboBoxItem Content="Положительный"/>
                                <ComboBoxItem Content="Сомнительный"/>
                            </ComboBox>
                        </StackPanel>

                        <StackPanel Grid.Column="2">
                            <TextBlock Text="ИФА HBsAg *" Style="{StaticResource FormLabel}"/>
                            <ComboBox x:Name="CmbHBsAg" Style="{StaticResource ModernComboBox}">
                                <ComboBoxItem Content="Отрицательный"/>
                                <ComboBoxItem Content="Положительный"/>
                                <ComboBoxItem Content="Сомнительный"/>
                            </ComboBox>
                        </StackPanel>
                    </Grid>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="10"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0">
                            <TextBlock Text="ИФА HCV *" Style="{StaticResource FormLabel}"/>
                            <ComboBox x:Name="CmbHCV" Style="{StaticResource ModernComboBox}">
                                <ComboBoxItem Content="Отрицательный"/>
                                <ComboBoxItem Content="Положительный"/>
                                <ComboBoxItem Content="Сомнительный"/>
                            </ComboBox>
                        </StackPanel>

                        <StackPanel Grid.Column="2">
                            <TextBlock Text="Сифилис *" Style="{StaticResource FormLabel}"/>
                            <ComboBox x:Name="CmbSyphilis" Style="{StaticResource ModernComboBox}">
                                <ComboBoxItem Content="Отрицательный"/>
                                <ComboBoxItem Content="Положительный"/>
                                <ComboBoxItem Content="Сомнительный"/>
                            </ComboBox>
                        </StackPanel>
                    </Grid>

                    <TextBlock Text="НАТ-тестирование (ПЦР)" Style="{StaticResource FormLabel}" FontWeight="Bold" Margin="0,10,0,5"/>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="10"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="10"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="ВИЧ" Style="{StaticResource FormLabel}"/>
                            <ComboBox x:Name="CmbNAT_HIV" Style="{StaticResource ModernComboBox}">
                                <ComboBoxItem Content="Отрицательный"/>
                                <ComboBoxItem Content="Положительный"/>
                                <ComboBoxItem Content="Не выполнялся"/>
                            </ComboBox>
                        </StackPanel>
                        <StackPanel Grid.Column="2">
                            <TextBlock Text="HBV" Style="{StaticResource FormLabel}"/>
                            <ComboBox x:Name="CmbNAT_HBV" Style="{StaticResource ModernComboBox}">
                                <ComboBoxItem Content="Отрицательный"/>
                                <ComboBoxItem Content="Положительный"/>
                                <ComboBoxItem Content="Не выполнялся"/>
                            </ComboBox>
                        </StackPanel>
                        <StackPanel Grid.Column="4">
                            <TextBlock Text="HCV" Style="{StaticResource FormLabel}"/>
                            <ComboBox x:Name="CmbNAT_HCV" Style="{StaticResource ModernComboBox}">
                                <ComboBoxItem Content="Отрицательный"/>
                                <ComboBoxItem Content="Положительный"/>
                                <ComboBoxItem Content="Не выполнялся"/>
                            </ComboBox>
                        </StackPanel>
                    </Grid>

                    <TextBlock Text="АЛТ (Ед/л)" Style="{StaticResource FormLabel}"/>
                    <TextBox x:Name="TxtAlt" Style="{StaticResource ModernTextBox}"/>

                    <TextBlock Text="Итоговый результат *" Style="{StaticResource FormLabel}"/>
                    <ComboBox x:Name="CmbOverall" Style="{StaticResource ModernComboBox}">
                        <ComboBoxItem Content="Годен"/>
                        <ComboBoxItem Content="Брак"/>
                    </ComboBox>

                    <TextBlock Text="Примечание" Style="{StaticResource FormLabel}"/>
                    <TextBox x:Name="TxtNotes" Style="{StaticResource ModernTextBox}" Height="50" TextWrapping="Wrap" AcceptsReturn="True"/>

                    <Button x:Name="BtnAdd" Content="Сохранить результаты" Style="{StaticResource PrimaryBtn}" Margin="0,10,0,15" Click="BtnAdd_Click"/>

                    <Button x:Name="BtnClear" Content="Очистить форму" Style="{StaticResource ActionBtn}" Margin="0,0,0,10" Click="BtnClear_Click"/>
                    <Button x:Name="BtnExport" Content="Экспорт в Excel" Style="{StaticResource PrimaryBtn}" Background="#10B981" Click="BtnExport_Click">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="6"/>
                            </Style>
                        </Button.Resources>
                    </Button>
                </StackPanel>
            </ScrollViewer>
        </Border>
    </Grid>
</Page>