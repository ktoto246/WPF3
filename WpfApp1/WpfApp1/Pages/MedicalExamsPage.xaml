<Page x:Class="WpfApp1.Pages.MedicalExamsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp1.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
     Title="Осмотры" Loaded="Page_Loaded" Background="#F3F4F6">

    <Page.Resources>
        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="Margin" Value="0,0,0,12"/>
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="BorderBrush" Value="#9CA3AF"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="6">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModernComboBox" TargetType="ComboBox">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="Margin" Value="0,0,0,12"/>
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="BorderBrush" Value="#9CA3AF"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <Style x:Key="ModernDatePicker" TargetType="DatePicker">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Margin" Value="0,0,0,12"/>
            <Setter Property="BorderBrush" Value="#9CA3AF"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <Style TargetType="TextBlock" x:Key="FormLabel">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#4B5563"/>
            <Setter Property="Margin" Value="0,0,0,5"/>
        </Style>

        <Style TargetType="Button" x:Key="ActionBtn">
            <Setter Property="Background" Value="#E5E7EB"/>
            <Setter Property="Foreground" Value="#374151"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#D1D5DB"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Button" x:Key="PrimaryBtn" BasedOn="{StaticResource ActionBtn}">
            <Setter Property="Background" Value="#DC2626"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#B91C1C"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="6.5*"/>
            <ColumnDefinition Width="3.5*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="White" CornerRadius="10" Margin="0,0,10,0">
            <Border.Effect>
                <DropShadowEffect BlurRadius="10" Direction="270" ShadowDepth="2" Opacity="0.05"/>
            </Border.Effect>

            <Grid Margin="15">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Text="Журнал медицинских осмотров" FontSize="18" FontWeight="Bold" Foreground="#1F2937" Margin="0,0,0,15"/>

                <DataGrid x:Name="ExamsGrid" Grid.Row="1" AutoGenerateColumns="False" 
                          IsReadOnly="True" HeadersVisibility="Column" GridLinesVisibility="Horizontal" 
                          HorizontalGridLinesBrush="#E5E7EB" BorderThickness="0"
                          RowBackground="White" AlternatingRowBackground="#F9FAFB" 
                          RowHeight="40" Background="Transparent" Foreground="#374151"
                          ScrollViewer.HorizontalScrollBarVisibility="Auto" ScrollViewer.VerticalScrollBarVisibility="Auto">
                    <DataGrid.Resources>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="#F3F4F6"/>
                            <Setter Property="FontWeight" Value="SemiBold"/>
                            <Setter Property="Foreground" Value="#4B5563"/>
                            <Setter Property="Padding" Value="10,8"/>
                        </Style>
                    </DataGrid.Resources>
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Дата осмотра" Binding="{Binding ExamDate, StringFormat=\{0:dd.MM.yyyy\}}" MinWidth="110"/>
                        <DataGridTextColumn Header="Донор" Binding="{Binding Donor.FullName}" MinWidth="180"/>
                        <DataGridTextColumn Header="Врач" Binding="{Binding Employee.FullName}" MinWidth="160"/>
                        <DataGridTextColumn Header="Результат осмотра" Binding="{Binding Result}" MinWidth="140"/>
                        <DataGridTextColumn Header="Гемоглобин (г/л)" Binding="{Binding HemoglobinGdl, StringFormat=\{0:F1\}}" MinWidth="130"/>
                        <DataGridTextColumn Header="Вес (кг)" Binding="{Binding WeightKg, StringFormat=\{0:F1\}}" MinWidth="80"/>
                        <DataGridTextColumn Header="Температура (°C)" Binding="{Binding TemperatureC, StringFormat=\{0:F1\}}" MinWidth="130"/>
                        <DataGridTextColumn Header="Пульс (уд/мин)" Binding="{Binding PulseBpm}" MinWidth="120"/>
                        <DataGridTemplateColumn Header="Давление (Сист. / Диаст.)" MinWidth="190">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock VerticalAlignment="Center" Padding="5,0,0,0">
                                        <Run Text="{Binding SystolicBP}"/><Run Text="/"/><Run Text="{Binding DiastolicBP}"/>
                                    </TextBlock>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTextColumn Header="Общий белок (г/л)" Binding="{Binding TotalProteinGdl, StringFormat=\{0:F1\}}" MinWidth="140"/>
                        <DataGridTextColumn Header="АЛТ (Ед/л)" Binding="{Binding AltUL, StringFormat=\{0:F1\}}" MinWidth="100"/>
                        <DataGridTextColumn Header="Причина отвода" Binding="{Binding RejectionReason}" MinWidth="150"/>
                        <DataGridTextColumn Header="Дополнительные пометки" Binding="{Binding Notes}" Width="*" MinWidth="180"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>

        <Border Grid.Column="1" Background="White" CornerRadius="10" Margin="10,0,0,0">
            <Border.Effect>
                <DropShadowEffect BlurRadius="10" Direction="270" ShadowDepth="2" Opacity="0.05"/>
            </Border.Effect>

            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel x:Name="FormPanel" Margin="20">
                    <TextBlock Text="Новый осмотр" FontSize="18" FontWeight="Bold" Foreground="#1F2937" Margin="0,0,0,15"/>

                    <TextBlock Text="Донор *" Style="{StaticResource FormLabel}"/>
                    <ComboBox x:Name="CmbDonor" Style="{StaticResource ModernComboBox}" IsEditable="True" IsTextSearchEnabled="True" DisplayMemberPath="FullName" SelectedValuePath="DonorId" SelectionChanged="CmbDonor_SelectionChanged"/>

                    <TextBlock Text="Дата осмотра *" Style="{StaticResource FormLabel}"/>
                    <DatePicker x:Name="DpExamDate" Style="{StaticResource ModernDatePicker}"/>

                    <TextBlock Text="Результат осмотра *" Style="{StaticResource FormLabel}"/>
                    <ComboBox x:Name="CmbResult" Style="{StaticResource ModernComboBox}" SelectionChanged="CmbResult_SelectionChanged">
                        <ComboBoxItem Content="Допущен"/>
                        <ComboBoxItem Content="Отведён"/>
                    </ComboBox>

                    <StackPanel x:Name="PnlRejection" Visibility="Collapsed">
                        <TextBlock Text="Причина отвода *" Style="{StaticResource FormLabel}"/>
                        <TextBox x:Name="TxtRejectionReason" Style="{StaticResource ModernTextBox}" Height="60" TextWrapping="Wrap" AcceptsReturn="True"/>
                    </StackPanel>

                    <StackPanel x:Name="PnlVitals" Visibility="Visible">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0">
                                <TextBlock Text="Гемоглобин (г/л) *" Style="{StaticResource FormLabel}"/>
                                <TextBox x:Name="TxtHemoglobin" Style="{StaticResource ModernTextBox}"/>
                            </StackPanel>
                            <StackPanel Grid.Column="2">
                                <TextBlock Text="Вес (кг) *" Style="{StaticResource FormLabel}"/>
                                <TextBox x:Name="TxtWeight" Style="{StaticResource ModernTextBox}"/>
                            </StackPanel>
                        </Grid>

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0">
                                <TextBlock Text="Температура (°C) *" Style="{StaticResource FormLabel}"/>
                                <TextBox x:Name="TxtTemperature" Style="{StaticResource ModernTextBox}"/>
                            </StackPanel>
                            <StackPanel Grid.Column="2">
                                <TextBlock Text="Пульс (уд/мин) *" Style="{StaticResource FormLabel}"/>
                                <TextBox x:Name="TxtPulse" Style="{StaticResource ModernTextBox}"/>
                            </StackPanel>
                        </Grid>

                        <TextBlock Text="Давление (Сист. / Диаст.) *" Style="{StaticResource FormLabel}"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBox x:Name="TxtSystolic" Grid.Column="0" Style="{StaticResource ModernTextBox}" ToolTip="Систолическое (верхнее)"/>
                            <TextBox x:Name="TxtDiastolic" Grid.Column="2" Style="{StaticResource ModernTextBox}" ToolTip="Диастолическое (нижнее)"/>
                        </Grid>

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0">
                                <TextBlock Text="Общий белок (г/л) *" Style="{StaticResource FormLabel}"/>
                                <TextBox x:Name="TxtProtein" Style="{StaticResource ModernTextBox}"/>
                            </StackPanel>
                            <StackPanel Grid.Column="2">
                                <TextBlock Text="АЛТ (Ед/л) *" Style="{StaticResource FormLabel}"/>
                                <TextBox x:Name="TxtAlt" Style="{StaticResource ModernTextBox}"/>
                            </StackPanel>
                        </Grid>
                    </StackPanel>

                    <TextBlock Text="Дополнительные пометки" Style="{StaticResource FormLabel}"/>
                    <TextBox x:Name="TxtNotes" Style="{StaticResource ModernTextBox}" Height="50" TextWrapping="Wrap" AcceptsReturn="True"/>

                    <Button x:Name="BtnAdd" Content="Зарегистрировать осмотр" Style="{StaticResource PrimaryBtn}" Margin="0,10,0,15" Click="BtnAdd_Click"/>

                    <Button x:Name="BtnClear" Content="Очистить форму" Style="{StaticResource ActionBtn}" Margin="0,0,0,10" Click="BtnClear_Click"/>
                    <Button x:Name="BtnExport" Content="Экспорт в Excel" Style="{StaticResource PrimaryBtn}" Background="#10B981" Click="BtnExport_Click">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="6"/>
                            </Style>
                        </Button.Resources>
                    </Button>
                </StackPanel>
            </ScrollViewer>
        </Border>
    </Grid>
</Page>