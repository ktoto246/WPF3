<Page x:Class="WpfApp1.Pages.DonorsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp1.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Доноры" Loaded="Page_Loaded" Background="#F3F4F6">

    <Page.Resources>
        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="Margin" Value="0,0,0,12"/>
            <Setter Property="Background" Value="#F9FAFB"/>
            <Setter Property="BorderBrush" Value="#D1D5DB"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="6">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModernComboBox" TargetType="ComboBox">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="Margin" Value="0,0,0,12"/>
            <Setter Property="Background" Value="#F9FAFB"/>
            <Setter Property="BorderBrush" Value="#D1D5DB"/>
        </Style>

        <Style x:Key="ModernDatePicker" TargetType="DatePicker">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Margin" Value="0,0,0,12"/>
        </Style>

        <Style TargetType="TextBlock" x:Key="FormLabel">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#4B5563"/>
            <Setter Property="Margin" Value="0,0,0,5"/>
        </Style>

        <Style TargetType="Button" x:Key="ActionBtn">
            <Setter Property="Background" Value="#E5E7EB"/>
            <Setter Property="Foreground" Value="#374151"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#D1D5DB"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Button" x:Key="PrimaryBtn" BasedOn="{StaticResource ActionBtn}">
            <Setter Property="Background" Value="#DC2626"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#B91C1C"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="6.5*"/>
            <ColumnDefinition Width="3.5*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="White" CornerRadius="10" Margin="0,0,10,0">
            <Border.Effect>
                <DropShadowEffect BlurRadius="10" Direction="270" ShadowDepth="2" Opacity="0.05"/>
            </Border.Effect>

            <Grid Margin="15">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Text="Реестр доноров отделения" FontSize="18" FontWeight="Bold" Foreground="#1F2937" Margin="0,0,0,15"/>

                <DataGrid x:Name="DonorsGrid" Grid.Row="1" AutoGenerateColumns="False" 
                          IsReadOnly="True" SelectionChanged="DonorsGrid_SelectionChanged"
                          HeadersVisibility="Column" GridLinesVisibility="Horizontal" 
                          HorizontalGridLinesBrush="#E5E7EB" BorderThickness="0"
                          RowBackground="White" AlternatingRowBackground="#F9FAFB" 
                          RowHeight="40" Background="Transparent" Foreground="#374151">
                    <DataGrid.Resources>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="#F3F4F6"/>
                            <Setter Property="FontWeight" Value="SemiBold"/>
                            <Setter Property="Foreground" Value="#4B5563"/>
                            <Setter Property="Padding" Value="10,8"/>
                        </Style>
                    </DataGrid.Resources>
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ФИО донора" Binding="{Binding FullName}" Width="*"/>
                        <DataGridTextColumn Header="Группа" Binding="{Binding BloodGroup}" Width="60"/>
                        <DataGridTextColumn Header="Резус" Binding="{Binding RhFactor}" Width="50"/>
                        <DataGridTextColumn Header="Kell" Binding="{Binding KellAntigen}" Width="60"/>
                        <DataGridTextColumn Header="Паспорт" Binding="{Binding PassportData}" Width="110"/>
                        <DataGridTextColumn Header="Статус" Binding="{Binding Status}" Width="120"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>

        <Border Grid.Column="1" Background="White" CornerRadius="10" Margin="10,0,0,0">
            <Border.Effect>
                <DropShadowEffect BlurRadius="10" Direction="270" ShadowDepth="2" Opacity="0.05"/>
            </Border.Effect>

            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel Margin="20">
                    <TextBlock Text="Карточка донора" FontSize="18" FontWeight="Bold" Foreground="#1F2937" Margin="0,0,0,15"/>

                    <TextBlock Text="ФИО донора *" Style="{StaticResource FormLabel}"/>
                    <TextBox x:Name="TxtFullName" Style="{StaticResource ModernTextBox}"/>

                    <TextBlock Text="Дата рождения *" Style="{StaticResource FormLabel}"/>
                    <DatePicker x:Name="DpBirthDate" Style="{StaticResource ModernDatePicker}"/>

                    <TextBlock Text="Паспортные данные (Серия Номер) *" Style="{StaticResource FormLabel}"/>
                    <TextBox x:Name="TxtPassport" Style="{StaticResource ModernTextBox}" ToolTip="Формат: XXXX XXXXXX"/>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="10"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0">
                            <TextBlock Text="Группа крови *" Style="{StaticResource FormLabel}"/>
                            <ComboBox x:Name="CmbBloodGroup" Style="{StaticResource ModernComboBox}">
                                <ComboBoxItem Content="I"/>
                                <ComboBoxItem Content="II"/>
                                <ComboBoxItem Content="III"/>
                                <ComboBoxItem Content="IV"/>
                            </ComboBox>
                        </StackPanel>

                        <StackPanel Grid.Column="2">
                            <TextBlock Text="Резус-фактор *" Style="{StaticResource FormLabel}"/>
                            <ComboBox x:Name="CmbRhFactor" Style="{StaticResource ModernComboBox}">
                                <ComboBoxItem Content="+"/>
                                <ComboBoxItem Content="-"/>
                            </ComboBox>
                        </StackPanel>
                    </Grid>

                    <TextBlock Text="Kell-антиген" Style="{StaticResource FormLabel}"/>
                    <ComboBox x:Name="CmbKell" Style="{StaticResource ModernComboBox}">
                        <ComboBoxItem Content="K+"/>
                        <ComboBoxItem Content="K-"/>
                        <ComboBoxItem Content="Kp^a"/>
                        <ComboBoxItem Content="Js^a"/>
                    </ComboBox>

                    <TextBlock Text="Контактный телефон" Style="{StaticResource FormLabel}"/>
                    <TextBox x:Name="TxtPhone" Style="{StaticResource ModernTextBox}" ToolTip="Формат: +7-XXX-XXX-XXXX"/>

                    <TextBlock Text="Медицинский статус *" Style="{StaticResource FormLabel}"/>
                    <ComboBox x:Name="CmbStatus" Style="{StaticResource ModernComboBox}" SelectionChanged="CmbStatus_SelectionChanged">
                        <ComboBoxItem Content="Активен"/>
                        <ComboBoxItem Content="Временное отстранение"/>
                        <ComboBoxItem Content="Постоянное отстранение"/>
                    </ComboBox>

                    <StackPanel x:Name="PnlDisqualified" Visibility="Collapsed">
                        <TextBlock Text="Отстранение действительно до *" Style="{StaticResource FormLabel}"/>
                        <DatePicker x:Name="DpDisqualifiedUntil" Style="{StaticResource ModernDatePicker}"/>
                    </StackPanel>

                    <TextBlock Text="Примечания / Причины отводов" Style="{StaticResource FormLabel}"/>
                    <TextBox x:Name="TxtNotes" Style="{StaticResource ModernTextBox}" Height="60" TextWrapping="Wrap" AcceptsReturn="True"/>

                    <Grid Margin="0,5,0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="10"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <Button x:Name="BtnAdd" Grid.Column="0" Content="Добавить" Style="{StaticResource PrimaryBtn}" Click="BtnAdd_Click"/>
                        <Button x:Name="BtnUpdate" Grid.Column="2" Content="Сохранить" Style="{StaticResource ActionBtn}" Click="BtnUpdate_Click"/>
                    </Grid>

                    <Separator Margin="0,5,0,10" Background="#E5E7EB"/>

                    <Button x:Name="BtnClear" Content="Очистить форму" Style="{StaticResource ActionBtn}" Margin="0,0,0,10" Click="BtnClear_Click"/>
                    <Button x:Name="BtnExport" Content="Экспорт в Excel" Style="{StaticResource PrimaryBtn}" Background="#10B981" Click="BtnExport_Click">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="6"/>
                            </Style>
                        </Button.Resources>
                    </Button>
                </StackPanel>
            </ScrollViewer>
        </Border>
    </Grid>
</Page>