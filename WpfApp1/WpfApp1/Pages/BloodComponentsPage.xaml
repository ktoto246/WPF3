<Page x:Class="WpfApp1.Pages.BloodComponentsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp1.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Компоненты крови" Loaded="Page_Loaded" Background="#F3F4F6">

    <Page.Resources>
        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="Margin" Value="0,0,0,12"/>
            <Setter Property="Background" Value="#F9FAFB"/>
            <Setter Property="BorderBrush" Value="#D1D5DB"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="6">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModernComboBox" TargetType="ComboBox">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="Margin" Value="0,0,0,12"/>
            <Setter Property="Background" Value="#F9FAFB"/>
            <Setter Property="BorderBrush" Value="#D1D5DB"/>
        </Style>

        <Style TargetType="TextBlock" x:Key="FormLabel">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#4B5563"/>
            <Setter Property="Margin" Value="0,0,0,5"/>
        </Style>

        <Style TargetType="Button" x:Key="ActionBtn">
            <Setter Property="Background" Value="#E5E7EB"/>
            <Setter Property="Foreground" Value="#374151"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#D1D5DB"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Button" x:Key="PrimaryBtn" BasedOn="{StaticResource ActionBtn}">
            <Setter Property="Background" Value="#DC2626"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#B91C1C"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="RadioButton" x:Key="ModernRadio">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#1F2937"/>
            <Setter Property="Margin" Value="0,0,15,0"/>
        </Style>
    </Page.Resources>

    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="6.5*"/>
            <ColumnDefinition Width="3.5*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="White" CornerRadius="10" Margin="0,0,10,0">
            <Border.Effect>
                <DropShadowEffect BlurRadius="10" Direction="270" ShadowDepth="2" Opacity="0.05"/>
            </Border.Effect>

            <Grid Margin="15">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Text="Склад компонентов крови" FontSize="18" FontWeight="Bold" Foreground="#1F2937" Margin="0,0,0,15"/>

                <DataGrid x:Name="ComponentsGrid" Grid.Row="1" AutoGenerateColumns="False" 
                          IsReadOnly="True" SelectionChanged="ComponentsGrid_SelectionChanged"
                          HeadersVisibility="Column" GridLinesVisibility="Horizontal" 
                          HorizontalGridLinesBrush="#E5E7EB" BorderThickness="0"
                          RowBackground="White" AlternatingRowBackground="#F9FAFB" 
                          RowHeight="40" Background="Transparent" Foreground="#374151">
                    <DataGrid.Resources>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="#F3F4F6"/>
                            <Setter Property="FontWeight" Value="SemiBold"/>
                            <Setter Property="Foreground" Value="#4B5563"/>
                            <Setter Property="Padding" Value="10,8"/>
                        </Style>
                    </DataGrid.Resources>
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Партия" Binding="{Binding LotNumber}" Width="120"/>
                        <DataGridTextColumn Header="Тип компонента" Binding="{Binding ComponentType}" Width="*"/>
                        <DataGridTextColumn Header="Объем (мл)" Binding="{Binding VolumeMl}" Width="90"/>
                        <DataGridTextColumn Header="Годен до" Binding="{Binding ExpirationDate, StringFormat=\{0:dd.MM.yyyy\}}" Width="90"/>
                        <DataGridTextColumn Header="Хранение" Binding="{Binding StorageLocation}" Width="120"/>
                        <DataGridTextColumn Header="Статус" Binding="{Binding Status}" Width="110"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>

        <Border Grid.Column="1" Background="White" CornerRadius="10" Margin="10,0,0,0">
            <Border.Effect>
                <DropShadowEffect BlurRadius="10" Direction="270" ShadowDepth="2" Opacity="0.05"/>
            </Border.Effect>

            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel Margin="20">
                    <TextBlock Text="Управление местом хранения" FontSize="16" FontWeight="Bold" Foreground="#1F2937" Margin="0,0,0,15"/>
                    <TextBlock Text="Текущее местоположение (макс. 100 симв.)" Style="{StaticResource FormLabel}"/>
                    <TextBox x:Name="TxtStorageLocation" Style="{StaticResource ModernTextBox}"/>
                    <Button x:Name="BtnUpdateLocation" Content="Обновить место хранения" Style="{StaticResource ActionBtn}" Margin="0,0,0,20" Click="BtnUpdateLocation_Click"/>

                    <Separator Margin="0,0,0,20" Background="#E5E7EB"/>

                    <TextBlock Text="Оформление выдачи / списания" FontSize="16" FontWeight="Bold" Foreground="#1F2937" Margin="0,0,0,15"/>

                    <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                        <RadioButton x:Name="RbIssue" Content="Выдача в ЛПУ" Style="{StaticResource ModernRadio}" IsChecked="True" Checked="RbOperation_Checked"/>
                        <RadioButton x:Name="RbWriteOff" Content="Списание (брак/срок)" Style="{StaticResource ModernRadio}" Checked="RbOperation_Checked"/>
                    </StackPanel>

                    <StackPanel x:Name="PnlRecipient" Visibility="Visible">
                        <TextBlock Text="Медицинское учреждение (Получатель) *" Style="{StaticResource FormLabel}"/>
                        <ComboBox x:Name="CmbRecipient" Style="{StaticResource ModernComboBox}" DisplayMemberPath="Name" SelectedValuePath="RecipientId"/>
                    </StackPanel>

                    <StackPanel x:Name="PnlWriteOff" Visibility="Collapsed">
                        <TextBlock Text="Причина списания *" Style="{StaticResource FormLabel}"/>
                        <ComboBox x:Name="CmbWriteOffReason" Style="{StaticResource ModernComboBox}">
                            <ComboBoxItem Content="Истёк срок"/>
                            <ComboBoxItem Content="Брак"/>
                            <ComboBoxItem Content="Бой"/>
                            <ComboBoxItem Content="Иное"/>
                        </ComboBox>
                    </StackPanel>

                    <TextBlock Text="Дополнительные комментарии" Style="{StaticResource FormLabel}"/>
                    <TextBox x:Name="TxtComments" Style="{StaticResource ModernTextBox}" Height="50" TextWrapping="Wrap" AcceptsReturn="True"/>

                    <Button x:Name="BtnProcess" Content="Подтвердить операцию" Style="{StaticResource PrimaryBtn}" Margin="0,10,0,15" Click="BtnProcess_Click"/>

                    <Button x:Name="BtnExport" Content="Экспорт в Excel" Style="{StaticResource PrimaryBtn}" Background="#10B981" Click="BtnExport_Click">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="6"/>
                            </Style>
                        </Button.Resources>
                    </Button>
                </StackPanel>
            </ScrollViewer>
        </Border>
    </Grid>
</Page>